import 'package:flutter/material.dart';

class AppColors {

  /// 🔴 Primary (YouTube Red)
  static const Color primary = Color(0xFFFF0000);

  /// 🔥 Slight soft red (buttons / highlights)
  static const Color primaryLight = Color(0xFFFF4D4D);

  /// ⚫ Dark Background (YouTube style)
  static const Color backgroundDark = Color(0xFF0F0F0F);

  /// ⚪ Light Background
  static const Color backgroundLight = Color(0xFFF9F9F9);

  /// 🎥 Card / Container (dark mode)
  static const Color cardDark = Color(0xFF1F1F1F);

  /// 🎥 Card / Container (light mode)
  static const Color cardLight = Colors.blueGrey;

  /// 📝 Text Colors
  static const Color textDark = Colors.black;
  static const Color textLight = Colors.white;
  static const Color textGrey = Color(0xFF9E9E9E);

  /// 🤖 AI Accent (modern touch)
  static const Color accent = Color(0xFF6C63FF);

  /// 🟢 Success
  static const Color success = Color(0xFF00C853);

  /// 🔴 Error
  static const Color error = Color(0xFFD50000);
}